/* globals describe, it */
'use strict'

var results = require('./lodashed')
var assert = require('assert')

describe('Florist', function () {
  it('works...', function () {
    assert(results === 9)
  })
})
